#!/usr/bin/env bash
set -euo pipefail

TARGET_DIR="$1"

# v0.3.4: Agent Teams — new agent templates installed via normal sync flow.
# No structural changes required in target projects.

printf '[0dai-repo] migration 0.3.3 -> 0.3.4 complete\n'
