# managed: true

# Claude Project Guide

Read `ai/docs/README.ai.md`, `ai/manifest/project.yaml`, and relevant `.claude/rules/` files before changing code.

Use hooks and rules as the deterministic policy layer for protected files and validation.
