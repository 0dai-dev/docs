# managed: true

# Codex Global Guidance

- Prefer files under `ai/docs/` and `ai/playbooks/` for repository understanding.
- Follow existing architecture before proposing structural rewrites.
- Keep changes aligned with the stack described in `ai/meta/project.manifest.yaml`.
