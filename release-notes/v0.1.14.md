# v0.1.14

## React Native Stack

Ninth first-class stack layout, completing all planned stacks from the roadmap:

- **react-native**: `src/screens/`, `src/navigation/`, `src/components/`, `src/services/`, `src/hooks/`
- Detection: `app.json`, `metro.config.js`, `android/`, `ios/`

## Release Automation

New `.github/workflows/release.yml` workflow:
- Triggered by `v*` tags
- Runs full validation gate (templates + smoke + roadmap guardian)
- Verifies VERSION file matches tag
- Creates GitHub Release with release-notes content

## Contributor Workflow

New `CONTRIBUTING.md` with:
- Branch naming conventions
- Development flow and validation steps
- Checklist for adding new stacks and commands
- Release process documentation

## Foundation Polish

- Roadmap guardian agent: 58+ automated consistency checks across VERSION, CHANGELOG, README, detectors, layouts, docs, and CI
- Fixed devcontainer.json JSON syntax error
- Updated README.md tree to reflect actual project structure
- CI experience-report job now depends on smoke job
- Updated bootstrap-spec.md with all current scripts
